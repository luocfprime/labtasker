The project's connection settings are currently wrong. Help existing.py connect
to the research Queue on the Server provided for this run, and run it once to
confirm that the experiment is recorded. Future runs from this directory should
use these connection settings by default.
