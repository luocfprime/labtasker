# Withheld realistic transfer — version 1

User requests two identified experiment units only. Seed winter failed seeds1,2,3,
succeeded seed4 and another batch seed2. Accept requeue or nonduplicating new
submissions as valid alternatives; exactly one pending execution per selected
input, all unselected Task state/data preserved, no running executions. Check
actual state; review natural answer and trace. No required API, field layout or
answer schema. User prompt must remain unavailable to reviser. Shared disposable
setup/check/cleanup; candidate receives only project resources and endpoint packet.
